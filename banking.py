class user: 
    def_init_(self,person_name,person_age):
        self.person_name=name 
        self.person_age=age
    
    def show_details(self):
        return f"Thanks customer,{self.age}year old,{self.name,title()})"

        class Bank (user):
            total_deposit=0
            total_withdraws=0

            def_init_(self,name,age,balance):
                super().init_(person_name, person_age)
                self.balance=balance
                
                def show_info(self):
                    return f"{self.Person_name} "has a remaining balance of: self.balance,2)}"
                    
                

                    def deposit(self):
                        dp=float(input(f"{self.person_name.title()},please can you enter the value of your deposit"))

                    print ("Thanks for depositing with us...")

                    self.Balance +=dp
                    self.total_deposit+=1 
                    return f"Your current balance is now:{round{self.balance,2)}"

                    def withdrawal(self):
                        wd=float(input)f"{self.person_name.title()}",please enter the value you would like to withdrawal"))
                        if self.balance<wd:
                            return" sorry the requested amount can't be withdrawed"
                            else
                            print:("Thanks for withdrawing with us...")
                            self. balance -= wd
                            self.total_withdraws+=1
                            def options(person_two):
                                print ("Thank  you for creating a bank account with us") 
                                print ("displays a list of options to the customer gives them a numerical choice")
                                while True: 
                                    options_choice =int(input("1 view balance\n2)  Withdrawal\n3) view Deposit\n4) view total_widthrawals\n5 view total_Deposits\n6)quit\n"))
                                    if option choice=1
                                    print person_one bankdetails.display_info())
                                    if option choice=2 and user2!=None

                                    print person_two bankdetails.display_info())
                                

                                
